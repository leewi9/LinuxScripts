# add below configuration in file /etc/security/limits.conf
* soft nofile 1024000
* hard nofile 1024000
* soft nproc 1024000
* hard nproc 1024000

# in order to change the limits for root user as well, it must be added explicitly:
root soft nofile 1024000
root hard nofile 1024000
root soft nproc 1024000
root hard nproc 1024000

# Add below configuration in /etc/sysctl.conf
fs.file-max = 1024000

# Add below configuraion in /etc/systemd/system.conf
DefaultLimitNOFILE=1024000

# Add below configuraion in /etc/systemd/user.conf
DefaultLimitNOFILE=1024000

# reload
/sbin/sysctl -p /etc/sysctl.conf
systemctl daemon-reload
# 貌似还是需要reboot

# 另外还可以，但是否有效果还需要再看看
# vim /etc/systemd/system/hysteria-server.service
# LimitNOFILE=1024000
